{
  "name": "simple-web-app",              // Имя проекта
  "version": "1.0.0",                    // Версия проекта
  "main": "app.js",                      // Главный файл приложения
  "scripts": {
    "start": "node app.js"               // Скрипт для запуска приложения
  },
  "dependencies": {
    "ejs": "^3.1.10",                    // Зависимость: EJS для шаблонов
    "express": "^4.19.2",                // Зависимость: Express для создания веб-сервера
    "pg": "^8.7.1"                       // Зависимость: pg для работы с PostgreSQL
  }
}
